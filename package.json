{
	"private": true,
	"scripts": {
		"build:lib": "pnpm --filter generate-design-token build",
		"test:lib": "pnpm --filter generate-design-token test",
		"npm:publish": "changeset publish",
		"npm:version": "changeset version"
	},
	"devDependencies": {
		"@changesets/changelog-github": "^0.5.0",
		"@changesets/cli": "^2.27.9"
	}
}

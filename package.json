{
	"private": true,
	"scripts": {
		"build:lib": "pnpm --filter generate-design-token build",
		"test:lib": "pnpm --filter generate-design-token test",
		"build:web": "pnpm --filter website build",
		"dev:web": "pnpm --filter website start",
		"publish": "changeset publish",
		"version": "changeset version"
	},
	"dependencies": {
		"generate-design-token": "^1.0.0"
	},
	"devDependencies": {
		"@changesets/changelog-github": "^0.5.0",
		"@changesets/cli": "^2.27.9"
	}
}
